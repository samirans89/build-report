# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

parameters:
- name: build_id
  displayName: Build Id
  type: string
  default: N/A

- name: username
  displayName: BrowserStack User Name
  type: string
  default: N/A

- name: access_key
  displayName: BrowserStack Access Key
  type: string
  default: N/A

trigger:
- master

pool:
  vmImage: 'ubuntu-latest'



steps:

- task: UseRubyVersion@0
  inputs:
    versionSpec: '>= 2.6'

- script: |
    gem install rest-client && gem install json gem install net/http
    ruby get_session_details.rb ${{ build_id }} ${{ username }} ${{ access_key }}
  displayName: 'Generate build report'



- task: PublishPipelineArtifact@1
  inputs:
    targetPath: '$(Pipeline.Workspace)'
    artifact: 'output.txt'
    publishLocation: 'pipeline'